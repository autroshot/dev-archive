---
interface Props {
  date: Date;
}

const { date } = Astro.props;
---

<time
  class="text-[15px] text-gray-500 dark:text-gray-400"
  datetime={date.toISOString()}
>
  {
    date.toLocaleDateString('ko-KR', {
      year: 'numeric',
      month: 'long',
      day: 'numeric',
    })
  }
</time>
